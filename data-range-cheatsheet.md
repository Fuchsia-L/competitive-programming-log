# 数据范围 → 复杂度 速查

> **"用数据范围反推复杂度"**：出题人精心调过 n，让正解能过、暴力 TLE。看 n 就能猜预期复杂度。

按 1 秒 ≈ 10⁸ 次操作估算：

| n 上限 | 预期复杂度 | 典型算法 |
|---|---|---|
| ≤ 11 | O(n!) | 全排列暴力 |
| ≤ 25 | O(2ⁿ) | 状压、子集枚举、Meet in the middle |
| ≤ 100 | O(n³) ~ O(n⁴) | Floyd、区间 DP |
| ≤ 500 | O(n³) | 矩阵 DP |
| **≤ 5000** | **O(n²)** | **二维 DP** |
| ≤ 10⁵ | O(n log n) | 排序、线段树、二分 |
| ≤ 10⁶ | O(n) 或 O(n log n) | 线性扫描、并查集 |
| ≤ 10⁹ | O(log n) 或 O(√n) | 二分、矩阵快速幂、根号分解 |

## 用法

**正向**：写完代码估算复杂度，对照表看合不合理。
- n = 10⁵ 写了 O(n²)？基本超时，重新想
- n = 100 写了 O(n)？大概率不是正解（暴力都能过，说明在错过更难的考点）

**反向**（更快）：先看数据范围圈定可能的复杂度，再去想"哪种算法能跑出这个复杂度"。直接把搜索空间砍掉一大半。

## 其他偏方

- **题面越长越简单**，越短越奇技淫巧
- **时间限制 ≥ 3s** 通常是放过常数大的写法（比如 set/map 而不是手写哈希）
- **多组数据 + 每组 n 大** → 单组复杂度要更严格（因为还要乘 T）
